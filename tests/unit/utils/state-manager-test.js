import stateMachine from 'ember-slides/utils/state-manager';

module('stateManager');

// Replace this with your real tests.
test('it works', function() {
  var result = stateMachine();
  ok(result);
});
