import stateMachine from 'ember-slide-deck/utils/state-manager';

module('stateManager');

// Replace this with your real tests.
test('it works', function() {
  var result = stateMachine.create();
  ok(result);
});
